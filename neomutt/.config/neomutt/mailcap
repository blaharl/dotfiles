text/html; mullvad-browser %s; test=test -n "$display"; needsterminal;
application/pdf; evince %s; test=test -n "$display"; needsterminal;

# HTML
text/html; w3m -I %{charset} -T text/html; copiousoutput;
text/plain; nvim %s

#Images
image/png; /usr/bin/feh %s
image/jpeg; /usr/bin/feh %s
